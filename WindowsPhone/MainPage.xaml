<Page
    x:Class="UdpPPTPhone.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UdpPPTPhone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="TB_IP" Margin="10,5" TextWrapping="Wrap" VerticalAlignment="Stretch"/>
        <Button Content="播放" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10,0" Grid.Row="1" FontSize="36" Tag="START" Click="Button_Click"/>
        <Button Content="停止" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10,0" Grid.Row="2" FontSize="36" Tag="ESC" Click="Button_Click"/>
        <Button Content="上一页" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10,0" Grid.Row="3" FontSize="36" Tag="PGUP" Click="Button_Click"/>
        <Button Content="下一页" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10,0" Grid.Row="4" FontSize="36" Tag="PGDN" Click="Button_Click"/>
    </Grid>
</Page>
